# Strategy registry. Source of truth for which strategies the paper engine runs.
# Edit and SIGHUP the paper trader to hot-reload.
#
# `sim_config` matches `polymarket-arb/scripts/mean_reversion/config.py::SimConfig`.

- id: cfg_21c8c00165b3
  name: "Validated #1 — DOWN-only deep dip (88% WR backtest)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "DOWN"
      entry_price_min: 0.075
      entry_price_max: 0.125
      drop_magnitude_pct: 15.0
      drop_window_sec: 30
      min_time_left_sec: 180
      proximity_max_pct: 0.5
      min_seconds_into_window: 15
    exit:
      profit_target_pct: 50.0
      stop_loss_pct: 80
      max_hold_sec: 180
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 15.0
      max_spread: 0.10
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: 25
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: cfg_333fde9cecb8
  name: "Validated #2 — BOTH ASIA hours, trailing (93% WR backtest)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.05
      entry_price_max: 0.15
      drop_magnitude_pct: 15.0
      drop_window_sec: 60
      min_time_left_sec: 180
      proximity_max_pct: 100.0
      min_seconds_into_window: 15
    exit:
      profit_target_pct: 75.0
      stop_loss_pct: 50
      max_hold_sec: 360
      trailing_stop_pct: 25
    filter:
      min_book_depth_usd: 50.0
      max_spread: 0.10
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ASIA"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: null
      post_loss_cooldown_sec: 180
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: relaxed_v1
  name: "Exploratory — wider band, no daily cap"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.05
      entry_price_max: 0.25
      drop_magnitude_pct: 10.0
      drop_window_sec: 60
      min_time_left_sec: 120
      proximity_max_pct: 100.0
      min_seconds_into_window: 15
    exit:
      profit_target_pct: 50.0
      stop_loss_pct: 50
      max_hold_sec: 600
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 15.0
      max_spread: 0.20
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 0
      concurrent_position_cap: null
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: cfg_5m_control
  name: "5m baseline — expected to lose (engine sanity check)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "5m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.05
      entry_price_max: 0.20
      drop_magnitude_pct: 15.0
      drop_window_sec: 30
      min_time_left_sec: 60
      proximity_max_pct: 1.0
      min_seconds_into_window: 15
    exit:
      profit_target_pct: 50.0
      stop_loss_pct: 50
      max_hold_sec: 120
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 15.0
      max_spread: 0.10
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

# ────────────────────────────────────────────────────────────────────────────
# "Human-intuition" shadow strategies. None of these are validated by the
# backtest. They're paper-only experiments to see which (if any) of the
# heuristics the user uses manually translate into separable PnL when made
# rule-based. Reviewed at end of week 1.
# ────────────────────────────────────────────────────────────────────────────

- id: cfg_velocity_v1
  name: "Velocity v1 — fast drops only (drop_window_sec=15)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "DOWN"
      entry_price_min: 0.075
      entry_price_max: 0.125
      drop_magnitude_pct: 12.0
      drop_window_sec: 15
      min_time_left_sec: 180
      proximity_max_pct: 0.5
      min_seconds_into_window: 15
    exit:
      profit_target_pct: 50.0
      stop_loss_pct: 80
      max_hold_sec: 180
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 15.0
      max_spread: 0.10
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: 25
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

# ────────────────────────────────────────────────────────────────────────────
# Backtest-tuned strategies (sweep on 5d of 15m data, Mar 15-17 + May 15-16).
# Each was a top-K finisher in scripts/backtest_sweep.py refinement pass.
# Started fresh 2026-05-16 — 1-week performance review scheduled by user.
# See data/analysis/BACKTEST_SUMMARY_2026-05-16.md for the full story.
# ────────────────────────────────────────────────────────────────────────────

- id: cfg_max_pnl_v1
  name: "Max PnL v1 — H1R 7248f0feace5 (92.6% WR, +$4962 backtest)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.20
      entry_price_max: 0.45
      drop_magnitude_pct: 5.0
      drop_window_sec: 90
      min_time_left_sec: 300
      proximity_max_pct: 100.0
      min_seconds_into_window: 60
    exit:
      profit_target_pct: 15.0
      stop_loss_pct: null
      max_hold_sec: 900
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 20.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: null
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 4
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: cfg_max_pnl_v2
  name: "Max PnL v2 — H1R 86fab33400af (90.9% WR, +$4945, target 25%)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.20
      entry_price_max: 0.40
      drop_magnitude_pct: 5.0
      drop_window_sec: 90
      min_time_left_sec: 300
      proximity_max_pct: 100.0
      min_seconds_into_window: 60
    exit:
      profit_target_pct: 25.0
      stop_loss_pct: null
      max_hold_sec: 900
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 20.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: null
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 4
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: cfg_max_pnl_v3
  name: "Max PnL v3 — H1R 04cc0e596f47 (92.3% WR, +$4754, widest band 0.50)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.20
      entry_price_max: 0.50
      drop_magnitude_pct: 5.0
      drop_window_sec: 90
      min_time_left_sec: 300
      proximity_max_pct: 100.0
      min_seconds_into_window: 60
    exit:
      profit_target_pct: 15.0
      stop_loss_pct: null
      max_hold_sec: 900
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 20.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: null
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 4
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: cfg_balanced_v1
  name: "Balanced v1 — HYB d2d33b9f959b (94.1% WR, +$2201, tight spread filter)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.25
      entry_price_max: 0.45
      drop_magnitude_pct: 8.0
      drop_window_sec: 90
      min_time_left_sec: 300
      proximity_max_pct: 100.0
      min_seconds_into_window: 60
    exit:
      profit_target_pct: 20.0
      stop_loss_pct: null
      max_hold_sec: 900
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 20.0
      max_spread: 0.005
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: null
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 4
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: cfg_high_vol_wr
  name: "High-vol WR — HYB 65b2db4037c1 (98.5% WR, +$901, vol=HIGH only)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.25
      entry_price_max: 0.45
      drop_magnitude_pct: 8.0
      drop_window_sec: 90
      min_time_left_sec: 300
      proximity_max_pct: 100.0
      min_seconds_into_window: 60
    exit:
      profit_target_pct: 20.0
      stop_loss_pct: null
      max_hold_sec: 900
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 20.0
      max_spread: 0.005
      book_imbalance_min: null
      vol_regime: "HIGH"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: null
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 4
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: cfg_late_panic_v1
  name: "Late-window panic — H7R 68333cac7a68 (77.2% WR, +$2279, last 5min only)"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.10
      entry_price_max: 0.20
      drop_magnitude_pct: 20.0
      drop_window_sec: 60
      min_time_left_sec: 60
      proximity_max_pct: 100.0
      min_seconds_into_window: 600
    exit:
      profit_target_pct: 45.0
      stop_loss_pct: null
      max_hold_sec: 240
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 20.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ALL"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.12
      daily_trade_cap: null
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 4
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.06
      use_next_tick_for_fill: true
      realistic_fill_model: true

# ───── Added 2026-05-17 from PROPOSED_STRATEGIES_2026-05-17.md ─────
# 8 GOLD picks (enabled) + 4 SILVER picks (disabled, paper-test only).
# Selected from a 3000-config LHS sweep with cross-segment + seed-stability gates.
# See `scripts/analysis/` for the pipeline that produced these.
- id: v2_gold_01_both_asia
  name: 'GOLD #1: BOTH/ASIA band=0.18-0.26 (sweep PnL $907)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: BOTH
      entry_price_min: 0.18
      entry_price_max: 0.255
      drop_magnitude_pct: 10.0
      drop_window_sec: 20
      min_time_left_sec: 180
      proximity_max_pct: 100.0
      min_seconds_into_window: 15
    exit:
      profit_target_pct: 120.0
      stop_loss_pct: null
      max_hold_sec: 600
      trailing_stop_pct: 20
    filter:
      min_book_depth_usd: 15.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: ALL
      time_of_day: ASIA
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: 50
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_gold_02_both_asia
  name: 'GOLD #2: BOTH/ASIA band=0.10-0.18 (sweep PnL $579)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: BOTH
      entry_price_min: 0.1
      entry_price_max: 0.18
      drop_magnitude_pct: 12.0
      drop_window_sec: 45
      min_time_left_sec: 300
      proximity_max_pct: 3.0
      min_seconds_into_window: 0
    exit:
      profit_target_pct: 50.0
      stop_loss_pct: null
      max_hold_sec: 720
      trailing_stop_pct: 25
    filter:
      min_book_depth_usd: 30.0
      max_spread: 0.12
      book_imbalance_min: null
      vol_regime: LOW
      time_of_day: ASIA
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_gold_03_down_all
  name: 'GOLD #3: DOWN/ALL band=0.10-0.25 (sweep PnL $739)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: DOWN
      entry_price_min: 0.1
      entry_price_max: 0.25
      drop_magnitude_pct: 10.0
      drop_window_sec: 90
      min_time_left_sec: 540
      proximity_max_pct: 100.0
      min_seconds_into_window: 0
    exit:
      profit_target_pct: 120.0
      stop_loss_pct: null
      max_hold_sec: 900
      trailing_stop_pct: 35
    filter:
      min_book_depth_usd: 150.0
      max_spread: 0.12
      book_imbalance_min: null
      vol_regime: ALL
      time_of_day: ALL
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: 25
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_gold_04_down_asia
  name: 'GOLD #4: DOWN/ASIA band=0.22-0.30 (sweep PnL $333)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: DOWN
      entry_price_min: 0.22
      entry_price_max: 0.3
      drop_magnitude_pct: 8.0
      drop_window_sec: 20
      min_time_left_sec: 300
      proximity_max_pct: 1.5
      min_seconds_into_window: 0
    exit:
      profit_target_pct: 90.0
      stop_loss_pct: null
      max_hold_sec: 720
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 15.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: MED
      time_of_day: ASIA
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_gold_05_up_overnight
  name: 'GOLD #5: UP/OVERNIGHT band=0.05-0.15 (sweep PnL $317)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: UP
      entry_price_min: 0.05
      entry_price_max: 0.15000000000000002
      drop_magnitude_pct: 18.0
      drop_window_sec: 60
      min_time_left_sec: 180
      proximity_max_pct: 0.2
      min_seconds_into_window: 60
    exit:
      profit_target_pct: 25.0
      stop_loss_pct: null
      max_hold_sec: 900
      trailing_stop_pct: 20
    filter:
      min_book_depth_usd: 150.0
      max_spread: 0.2
      book_imbalance_min: null
      vol_regime: LOW
      time_of_day: OVERNIGHT
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_gold_06_up_overnight
  name: 'GOLD #6: UP/OVERNIGHT band=0.10-0.30 (sweep PnL $217)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: UP
      entry_price_min: 0.1
      entry_price_max: 0.30000000000000004
      drop_magnitude_pct: 10.0
      drop_window_sec: 30
      min_time_left_sec: 420
      proximity_max_pct: 0.2
      min_seconds_into_window: 0
    exit:
      profit_target_pct: 80.0
      stop_loss_pct: null
      max_hold_sec: 900
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 50.0
      max_spread: 0.12
      book_imbalance_min: null
      vol_regime: LOW
      time_of_day: OVERNIGHT
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: 25
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_gold_07_down_asia
  name: 'GOLD #7: DOWN/ASIA band=0.18-0.24 (sweep PnL $298)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: DOWN
      entry_price_min: 0.18
      entry_price_max: 0.24
      drop_magnitude_pct: 12.0
      drop_window_sec: 15
      min_time_left_sec: 180
      proximity_max_pct: 0.4
      min_seconds_into_window: 60
    exit:
      profit_target_pct: 70.0
      stop_loss_pct: null
      max_hold_sec: 720
      trailing_stop_pct: 35
    filter:
      min_book_depth_usd: 10.0
      max_spread: 0.08
      book_imbalance_min: null
      vol_regime: ALL
      time_of_day: ASIA
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_gold_08_both_asia
  name: 'GOLD #8: BOTH/ASIA band=0.18-0.24 (sweep PnL $159)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: BOTH
      entry_price_min: 0.18
      entry_price_max: 0.24
      drop_magnitude_pct: 35.0
      drop_window_sec: 30
      min_time_left_sec: 120
      proximity_max_pct: 3.0
      min_seconds_into_window: 30
    exit:
      profit_target_pct: 70.0
      stop_loss_pct: null
      max_hold_sec: 360
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 15.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: MED
      time_of_day: ASIA
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_silver_01_both_overnight
  name: 'SILVER #1: BOTH/OVERNIGHT (sweep PnL $980)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: BOTH
      entry_price_min: 0.15
      entry_price_max: 0.22499999999999998
      drop_magnitude_pct: 10.0
      drop_window_sec: 90
      min_time_left_sec: 300
      proximity_max_pct: 0.2
      min_seconds_into_window: 30
    exit:
      profit_target_pct: 120.0
      stop_loss_pct: 50
      max_hold_sec: 120
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 50.0
      max_spread: 0.08
      book_imbalance_min: null
      vol_regime: ALL
      time_of_day: OVERNIGHT
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.08
      daily_trade_cap: 50
      post_loss_cooldown_sec: 0
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_silver_02_down_asia
  name: 'SILVER #2: DOWN/ASIA (sweep PnL $510)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: DOWN
      entry_price_min: 0.18
      entry_price_max: 0.24
      drop_magnitude_pct: 8.0
      drop_window_sec: 20
      min_time_left_sec: 120
      proximity_max_pct: 1.5
      min_seconds_into_window: 60
    exit:
      profit_target_pct: 120.0
      stop_loss_pct: null
      max_hold_sec: 240
      trailing_stop_pct: 15
    filter:
      min_book_depth_usd: 10.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: ALL
      time_of_day: ASIA
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_silver_03_up_asia
  name: 'SILVER #3: UP/ASIA (sweep PnL $457)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: UP
      entry_price_min: 0.18
      entry_price_max: 0.26
      drop_magnitude_pct: 8.0
      drop_window_sec: 15
      min_time_left_sec: 180
      proximity_max_pct: 1.5
      min_seconds_into_window: 30
    exit:
      profit_target_pct: 50.0
      stop_loss_pct: null
      max_hold_sec: 600
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 30.0
      max_spread: 0.12
      book_imbalance_min: null
      vol_regime: MED
      time_of_day: ASIA
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true
- id: v2_silver_04_up_asia
  name: 'SILVER #4: UP/ASIA (sweep PnL $438)'
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: 15m
  sim_config:
    entry:
      side: UP
      entry_price_min: 0.13
      entry_price_max: 0.21000000000000002
      drop_magnitude_pct: 8.0
      drop_window_sec: 30
      min_time_left_sec: 420
      proximity_max_pct: 100.0
      min_seconds_into_window: 0
    exit:
      profit_target_pct: 120.0
      stop_loss_pct: 60
      max_hold_sec: 720
      trailing_stop_pct: 15
    filter:
      min_book_depth_usd: 10.0
      max_spread: 0.05
      book_imbalance_min: null
      vol_regime: ALL
      time_of_day: ASIA
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.0
      reaction_delay_max_sec: 2.5
      signal_skip_prob: 0.0
      daily_trade_cap: null
      post_loss_cooldown_sec: 60
      concurrent_position_cap: 1
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.05
      use_next_tick_for_fill: true
      realistic_fill_model: true

# ─────────────────────────────────────────────────────────────────────
# sweep_v2 — trail-v2 forward-test (added 2026-05-23)
#
# Base config: 1be3e4e39192 — lenient survivor of the 2,665-config sweep,
# cleared 6-axis stress + 4-day walk-forward. Same params, three exit
# variants to forward-test trailing-stop activation thresholds:
#
#  - sv2_baseline_no_trail   : no trailing stop (engine TP/SL/max_hold only)
#  - sv2_trail_act100_lock30 : trail engages once peak ≥ +100% PnL, lock at peak−30%
#  - sv2_trail_3step         : 3-step staircase (50/+40 → 100/+25 → 200/+15)
#
# Backtest projections at $10/trade bet size (9 days May 2026):
#  baseline:    Sharpe 0.92, $40/day, ~$1,200/month
#  act100/30:   Sharpe 1.27, $53/day, ~$1,594/month   (3/4 symbols+, best Sharpe)
#  3-step:      Sharpe 1.12, $63/day, ~$1,885/month   (4/4 symbols+, best PnL)
#
# Win rate 45% — strategy relies on 270% profit target to pay for losses.
# All 3 enabled for paper-only forward validation. NO real money.
# ─────────────────────────────────────────────────────────────────────

- id: sv2_baseline_no_trail
  name: "sweep_v2 baseline (no trail) — paper forward test"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.09031478099581848
      entry_price_max: 0.3189990074178029
      drop_magnitude_pct: 15.038191127543117
      drop_window_sec: 296
      min_time_left_sec: 223
      proximity_max_pct: 93.17352957482386
      min_seconds_into_window: 94
    exit:
      profit_target_pct: 269.98340725575076
      stop_loss_pct: null
      max_hold_sec: 537
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 76.45258989284731
      max_spread: 0.02808037554116574
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ASIA"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.0
      signal_skip_prob: 0.028915951978647072
      daily_trade_cap: 20
      post_loss_cooldown_sec: 0
      concurrent_position_cap: null
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.09016200067936624
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: sv2_trail_act100_lock30
  name: "sweep_v2 trail-v2 act100/lock30 (single-step) — paper forward test"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  trail_v2_staircase:
    - [100, 30]
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.09031478099581848
      entry_price_max: 0.3189990074178029
      drop_magnitude_pct: 15.038191127543117
      drop_window_sec: 296
      min_time_left_sec: 223
      proximity_max_pct: 93.17352957482386
      min_seconds_into_window: 94
    exit:
      profit_target_pct: 269.98340725575076
      stop_loss_pct: null
      max_hold_sec: 537
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 76.45258989284731
      max_spread: 0.02808037554116574
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ASIA"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.0
      signal_skip_prob: 0.028915951978647072
      daily_trade_cap: 20
      post_loss_cooldown_sec: 0
      concurrent_position_cap: null
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.09016200067936624
      use_next_tick_for_fill: true
      realistic_fill_model: true

- id: sv2_trail_3step
  name: "sweep_v2 trail-v2 3-step (50/40 → 100/25 → 200/15) — paper forward test"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  trail_v2_staircase:
    - [50, 40]
    - [100, 25]
    - [200, 15]
  sim_config:
    entry:
      side: "BOTH"
      entry_price_min: 0.09031478099581848
      entry_price_max: 0.3189990074178029
      drop_magnitude_pct: 15.038191127543117
      drop_window_sec: 296
      min_time_left_sec: 223
      proximity_max_pct: 93.17352957482386
      min_seconds_into_window: 94
    exit:
      profit_target_pct: 269.98340725575076
      stop_loss_pct: null
      max_hold_sec: 537
      trailing_stop_pct: null
    filter:
      min_book_depth_usd: 76.45258989284731
      max_spread: 0.02808037554116574
      book_imbalance_min: null
      vol_regime: "ALL"
      time_of_day: "ASIA"
      multi_tier_entry: 1
      correlated_signal_filter: false
    human:
      reaction_delay_min_sec: 0.5
      reaction_delay_max_sec: 2.0
      signal_skip_prob: 0.028915951978647072
      daily_trade_cap: 20
      post_loss_cooldown_sec: 0
      concurrent_position_cap: null
      fixed_bet_usd: 10.0
    fill:
      fee_rate: 0.07
      reject_prob: 0.09016200067936624
      use_next_tick_for_fill: true
      realistic_fill_model: true

# ─────────────────────────────────────────────────────────────────────
# sweep_v2 — 4 additional trail-v2 stress-survivors (added 2026-05-23)
# All share the same base config 1be3e4e39192; differ only in staircase.
#
#  - sv2_trail_act150_lock30        : single-step, activate +150% lock peak−30% (best adv-cost survivor +$399)
#  - sv2_trail_act30_lock30         : single-step, activate +30%  lock peak−30% (highest Sharpe 1.31)
#  - sv2_trail_act50_30__act150_15  : two-step (50/30 → 150/15)              (smoothest joint perturbation)
#  - sv2_trail_act100_25__act200_15 : two-step (100/25 → 200/15)             (cost-aware variant)
# ─────────────────────────────────────────────────────────────────────

- id: sv2_trail_act150_lock30
  name: "sweep_v2 trail-v2 act150/lock30 (single-step) — paper forward test"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  trail_v2_staircase:
    - [150, 30]
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.09031478099581848, entry_price_max: 0.3189990074178029, drop_magnitude_pct: 15.038191127543117, drop_window_sec: 296, min_time_left_sec: 223, proximity_max_pct: 93.17352957482386, min_seconds_into_window: 94}
    exit:  {profit_target_pct: 269.98340725575076, stop_loss_pct: null, max_hold_sec: 537, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 76.45258989284731, max_spread: 0.02808037554116574, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ASIA", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.5, reaction_delay_max_sec: 2.0, signal_skip_prob: 0.028915951978647072, daily_trade_cap: 20, post_loss_cooldown_sec: 0, concurrent_position_cap: null, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.09016200067936624, use_next_tick_for_fill: true, realistic_fill_model: true}

- id: sv2_trail_act30_lock30
  name: "sweep_v2 trail-v2 act30/lock30 (single-step, highest Sharpe) — paper forward test"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  trail_v2_staircase:
    - [30, 30]
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.09031478099581848, entry_price_max: 0.3189990074178029, drop_magnitude_pct: 15.038191127543117, drop_window_sec: 296, min_time_left_sec: 223, proximity_max_pct: 93.17352957482386, min_seconds_into_window: 94}
    exit:  {profit_target_pct: 269.98340725575076, stop_loss_pct: null, max_hold_sec: 537, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 76.45258989284731, max_spread: 0.02808037554116574, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ASIA", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.5, reaction_delay_max_sec: 2.0, signal_skip_prob: 0.028915951978647072, daily_trade_cap: 20, post_loss_cooldown_sec: 0, concurrent_position_cap: null, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.09016200067936624, use_next_tick_for_fill: true, realistic_fill_model: true}

- id: sv2_trail_act50_30__act150_15
  name: "sweep_v2 trail-v2 2-step (50/30 → 150/15) — paper forward test"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  trail_v2_staircase:
    - [50, 30]
    - [150, 15]
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.09031478099581848, entry_price_max: 0.3189990074178029, drop_magnitude_pct: 15.038191127543117, drop_window_sec: 296, min_time_left_sec: 223, proximity_max_pct: 93.17352957482386, min_seconds_into_window: 94}
    exit:  {profit_target_pct: 269.98340725575076, stop_loss_pct: null, max_hold_sec: 537, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 76.45258989284731, max_spread: 0.02808037554116574, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ASIA", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.5, reaction_delay_max_sec: 2.0, signal_skip_prob: 0.028915951978647072, daily_trade_cap: 20, post_loss_cooldown_sec: 0, concurrent_position_cap: null, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.09016200067936624, use_next_tick_for_fill: true, realistic_fill_model: true}

- id: sv2_trail_act100_25__act200_15
  name: "sweep_v2 trail-v2 2-step (100/25 → 200/15) — paper forward test"
  enabled: false
  starting_capital_usd: 1000.0
  timeframe: "15m"
  trail_v2_staircase:
    - [100, 25]
    - [200, 15]
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.09031478099581848, entry_price_max: 0.3189990074178029, drop_magnitude_pct: 15.038191127543117, drop_window_sec: 296, min_time_left_sec: 223, proximity_max_pct: 93.17352957482386, min_seconds_into_window: 94}
    exit:  {profit_target_pct: 269.98340725575076, stop_loss_pct: null, max_hold_sec: 537, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 76.45258989284731, max_spread: 0.02808037554116574, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ASIA", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.5, reaction_delay_max_sec: 2.0, signal_skip_prob: 0.028915951978647072, daily_trade_cap: 20, post_loss_cooldown_sec: 0, concurrent_position_cap: null, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.09016200067936624, use_next_tick_for_fill: true, realistic_fill_model: true}
# NOT live yet (enabled:false). The Phase 1 edge needs the FRESH WS spot (live_spot)
# for distance-to-strike, but the live tick's coinbase_price/move_pct is the STALE
# ~14s poll — median 1.75bps off, sign-disagrees 12.8%, fatal at a 5bps threshold.
# Forward-validate via the daily OOS backtest (research.forward_validate, fresh
# cb_spot) until the WS spot is wired into the paper engine (pre-live requirement).
- id: det_lwd_v1
  name: "Determinism late-window pickoff v1 (Phase 1 edge: last 60s, dist>=5bps, fav ask<=0.90, hold; OOS +$1.68/tr 91% WR)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {t_min_sec: 1, t_max_sec: 60, dist_min_bps: 5.0, max_ask: 0.90, min_ask: 0.50}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
- id: det_lwd_v1_capped
  name: "Determinism late-window pickoff v1 — $50/day max-loss cap (live-candidate config)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {t_min_sec: 1, t_max_sec: 60, dist_min_bps: 5.0, max_ask: 0.90, min_ask: 0.50, max_daily_loss_usd: 50.0}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
- id: det_sqp_v1
  name: "Stale-quote pickoff v1 (Phase 2: mid-window, |model_p-mid| in [0.08,0.30] + spot jump>=8bps, hold). Higher-variance secondary edge."
  enabled: true
  type: stale_quote
  starting_capital_usd: 1000.0
  timeframe: "15m"
  stale_quote: {curve_file: "data/research/stale_quote_curve.json", margin: 0.08, max_mispricing: 0.30, jump_bps: 8.0, t_lo_left: 60, t_hi_left: 840, min_ask: 0.05, max_ask: 0.95}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.05, entry_price_max: 0.95, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
- id: det_sqp_v1_capped
  name: "Stale-quote pickoff v1 — $50/day max-loss cap (higher-variance; live-candidate w/ risk control)"
  enabled: true
  type: stale_quote
  starting_capital_usd: 1000.0
  timeframe: "15m"
  stale_quote: {curve_file: "data/research/stale_quote_curve.json", margin: 0.08, max_mispricing: 0.30, jump_bps: 8.0, t_lo_left: 60, t_hi_left: 840, min_ask: 0.05, max_ask: 0.95, max_daily_loss_usd: 50.0}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.05, entry_price_max: 0.95, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}

# ── v2 loss-filtered strategies (docs/research/loss_pattern_filters.md, 2026-05-30) ──
# Same edges as det_lwd_v1 / det_sqp_v1 with the validated loss-avoidance filters
# layered on. v1 stays enabled as the unfiltered control for A/B comparison.
- id: det_lwd_v2
  name: "Determinism v2 — v1 + loss filters (max_ask 0.88, skip adverse_vel>2bps, require strike_crossings>=1)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {t_min_sec: 1, t_max_sec: 60, dist_min_bps: 5.0, max_ask: 0.88, min_ask: 0.50, adverse_vel_max_bps: 2.0, min_strike_crossings: 1}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
- id: det_sqp_v2
  name: "Stale-quote v2 — v1 + loss filters (margin 0.12, skip dist>19bps)"
  enabled: true
  type: stale_quote
  starting_capital_usd: 1000.0
  timeframe: "15m"
  stale_quote: {curve_file: "data/research/stale_quote_curve.json", margin: 0.12, max_mispricing: 0.30, jump_bps: 8.0, t_lo_left: 60, t_hi_left: 840, min_ask: 0.05, max_ask: 0.95, max_dist_bps: 19.0}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.05, entry_price_max: 0.95, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# ===== NEW 2026-06-05 — verified improvements (docs/research/IMPROVEMENT_FINDINGS_2026-06-04.md) =====
# E4: disagreement-determinism (paper). Last 60s, book favourite DISAGREES with spot,
# |dist|>=5bps -> buy the CHEAP spot-implied side the book has as underdog, hold.
# Verified +$13-36/tr OOS, 84% WR, all 4 coins, 15/15 CPCV folds. Capacity-limited (thin books).
- id: det_disagree_v1
  name: "E4 disagreement-determinism (fade the book, buy cheap spot side, last 60s)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {t_min_sec: 1, t_max_sec: 60, dist_min_bps: 5.0, max_ask: 0.90, min_ask: 0.05, mode: "disagree"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.05, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# LIVE probe: det_lwd_v2 config + real-money executor bridge (live:true emits intents
# to data/live/intents.jsonl). $50/day hard_worstcase cap (free safety; det never trips it).
# The standalone executor (scripts/live_executor.py, Python 3.11) places the real orders.
- id: det_lwd_live
  name: "Determinism v2 LIVE probe ($5/trade real money; dist>=8bps locked-favourite filter for Chainlink; $25/day hard cap)"
  enabled: true
  live: true
  type: determinism
  starting_capital_usd: 100.0
  timeframe: "15m"
  determinism: {t_min_sec: 1, t_max_sec: 60, dist_min_bps: 8.0, max_ask: 0.88, min_ask: 0.50, adverse_vel_max_bps: 2.0, min_strike_crossings: 1, max_daily_loss_usd: 25.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 5.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# ===== NEW 2026-06-05 — VERIFIED favourite-value edges (docs/research/CHAINLINK_EDGE_HUNT_2026-06-05.md) =====
# Mechanism: the book systematically UNDERPRICES near-locked favourites (favourite-longshot bias).
# All decide with a TIME BUFFER + hold to resolution -> LATENCY-PROOF (a laptop trader keeps it).
# Settled on Chainlink, depth-gated, fresh-OOS positive + latency-flat. Nearly disjoint (Jaccard<0.1);
# combined fresh-OOS +$0.90/tr, DSR 0.97. PAPER forward-test (1 week) before any live decision.
- id: fav_lowvol
  name: "Fav-value: low-vol cheap-favourite (verified +$0.97 fresh-OOS CL; mid-window 4-7min, hold)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "consistent", t_min_sec: 240, t_max_sec: 420, dist_min_bps: 8.0, min_ask: 0.55, max_ask: 0.80, vol_max_bps: 1.0, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.55, entry_price_max: 0.80, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
- id: fav_deepdown
  name: "Fav-value: deep DOWN-favourite 7-8min (verified +$0.66 fresh-OOS CL; buy NO, hold)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "consistent", t_min_sec: 420, t_max_sec: 480, dist_min_bps: 0.0, min_ask: 0.88, max_ask: 0.93, restrict_fav_side: "no", max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.88, entry_price_max: 0.93, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
- id: fav_momentum
  name: "Fav-value: late-window momentum-continuation (verified +$0.88 fresh-OOS CL; decide 60-120s, buy fav, hold)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "consistent", t_min_sec: 60, t_max_sec: 120, dist_min_bps: 12.0, min_ask: 0.50, max_ask: 0.90, adverse_vel_max_bps: 0.0, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
- id: fav_disagree
  name: "Fav-value (E4-hardened): mid-window disagree-fade (verified +$5.39 fresh-OOS CL; tl 120-360s, |dist|>=10, buy spot-implied side, hold)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "disagree", t_min_sec: 120, t_max_sec: 360, dist_min_bps: 10.0, min_ask: 0.05, max_ask: 0.90, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.05, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# ===== NEW 2026-06-11 — oracle-night paper twins (docs/research/EDGE_ATLAS_2026-06-10.md,
# SWEEP_LIVECOST_2026-06-10.md). Zero-code forward tests via the existing disagree mode. =====
# EARLY-DISAGREE (the atlas's one unharvested family): same disagreement signal as fav_disagree
# but EARLIER (tl 450-900s vs 120-360) at cheap-side ask 0.30-0.45 — future +20.9%/$1 [15.2,26.4],
# n=1279, 9/9 days, 4/4 coins, ~142 windows/day, <=0.30 covered by anything deployed. APPROXIMATION:
# the atlas family also required |cl_dist|<12bps; this twin omits that condition (engine's disagree
# mode has no cl ceiling gate) — compare its forward run to the atlas cells before trusting.
- id: early_disagree_v1
  name: "Early-disagree paper (atlas family: tl 450-900s, |dist|>=10bps, buy spot side ask 0.30-0.45, hold; future +20.9%/$1; NO cl_dist<12 gate - approximation)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "disagree", t_min_sec: 450, t_max_sec: 900, dist_min_bps: 10.0, min_ask: 0.30, max_ask: 0.45, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.30, entry_price_max: 0.45, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# LIVE since 2026-06-11 (user-approved early deploy, ~4 days ahead of the normal 1-week paper
# test): the atlas family under real money. Rationale for the exception: 2nd-strongest validated
# edge (fills_live guarded future +$5.31/fill [3.71,6.81] @$10, +$2.35 @$5), IDENTICAL code path
# to the battle-tested fav_disagree_live (same disagree mode/intents/executor — only params
# differ), and the friendliest execution profile on the board (entry 7.5-15min before settle, no
# race). $50/day cap per the positive-skew H4 logic. Guards SHADOW for this sid (not in
# ENFORCE_SIDS). Risk: skips the paper forward test — day one may be ugly; caps bound it.
- id: early_disagree_live
  name: "Early-disagree LIVE ($5/trade real money; tl 450-900s, |dist|>=10bps, buy spot side ask 0.30-0.45, hold; fills_live future +$2.35/fill @$5; $50/day hard cap, $100 bankroll)"
  enabled: true
  live: false   # DEMOTED 2026-06-18 reckoning: clean opportunity collapsed 41.5->3.4/actday + losing real money (-$38 in 3 days). Paper twin continues.
  type: determinism
  starting_capital_usd: 100.0
  timeframe: "15m"
  determinism: {mode: "disagree", t_min_sec: 450, t_max_sec: 900, dist_min_bps: 10.0, min_ask: 0.30, max_ask: 0.45, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.30, entry_price_max: 0.45, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 5.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# EDGE HUNT v2 T3 follow-up (pre-registered test_ledger 2026-07-03): the honest atlas's 4
# virgin-confirmed cells are all cheap-DISAGREE at tl 300-900s ask 0.30-0.40 conditioned on
# CHAINLINK distance 2-12bps (the early_disagree twins gated Coinbase dist>=10 instead — the
# wrong oracle for the gate). PAPER twin; standard gate (>=14 virgin-forward days,
# official-settled CI-lo>0 via nightly scoreboard) before any live talk.
- id: early_disagree_cl_v1
  name: "Early-disagree CL-gated (atlas v2 cells: tl 450-900s, disagree, ask 0.30-0.40, Chainlink agree+dist>=2bps, hold)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "disagree", t_min_sec: 450, t_max_sec: 900, dist_min_bps: 0.0, min_ask: 0.30, max_ask: 0.40, oracle_gate: "agree_and_dist", cl_dist_min_bps: 2.0, max_daily_loss_usd: 10000.0, daily_loss_mode: "soft_settled"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.30, entry_price_max: 0.40, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 5.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}

# FAV_DISAGREE dist 10->5 widening (sweep e4_1070: future +$2.21/fill live-model, covers 94.5% of
# the deployed fav_disagree set + new volume). A/B twin vs the running fav_disagree.
- id: fav_disagree_d5
  name: "Fav-disagree d5 paper (A/B: dist gate 10->5bps widening; sweep e4_1070 future +$2.21/fill live-model)"
  enabled: true
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "disagree", t_min_sec: 120, t_max_sec: 360, dist_min_bps: 5.0, min_ask: 0.05, max_ask: 0.90, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.05, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# LIVE since 2026-06-10 (user-approved after the live-fill-model re-judging): the strongest edge on
# the board under REAL fill physics. Band tightened 0.90->0.45 from the forward-paper entry-bucket
# EV (0.50-0.90 entries LOSE: -$29/24tr 46% WR; edge lives <=0.45; 0.05-0.20 jackpot tail kept per
# sq-deep-tail-floor lesson) + live-model rejudge (ask<=0.45: future +$3.54/fill [1.99,5.11] vs
# +$1.67 at 0.90). 0.45 also bounds the executor chase band + lets the notional guard deploy the
# full $5 at typical ~0.40 entries. Guards stay SHADOW for this sid (floor-abort semantics were
# calibrated on det; an ask drop is ambiguous for disagree — collect its own cohort first).
- id: fav_disagree_live
  name: "Fav-disagree LIVE ($5/trade real money; tl 120-360s, |dist|>=10bps, buy spot side ask 0.05-0.45, hold; live-model future +$3.54/fill; $50/day hard cap, $100 bankroll)"
  enabled: true
  live: true    # RE-ARMED 2026-07-02 (Edge Hunt v2, user-approved): passed ALL honest gates — official labels, virgin BH-FDR (p=.00025), consistency leg (+$1.93/fill full clean-future), live-2 guarded fills (+$2.57/fill, 56% fill, seed-robust). The 06-18 kill was an n=34 small-sample verdict. Stop rule: official-settled <= -$0.50/fill with CI-hi<0.
  type: determinism
  starting_capital_usd: 100.0
  timeframe: "15m"
  # $50/day (not $25): 60% WR x ~3:1 payoffs = sq-shaped positive skew; H4 (daily_cap research)
  # showed tight caps truncate exactly this profile (a $25 cap trips on ~1-in-10 normal-variance
  # days). At ~8 fills/day max realistic deployment is ~$40, so $50 only binds on a wild-volume
  # catastrophe day — tail guard, not a skew tax. Bankroll $100 stays the hard backstop.
  determinism: {mode: "disagree", t_min_sec: 120, t_max_sec: 360, dist_min_bps: 10.0, min_ask: 0.05, max_ask: 0.45, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.05, entry_price_max: 0.45, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 5.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# ===== NEW 2026-06-05 — 2099-hypothesis sweep survivor (docs/research/HYPOTHESIS_SWEEP_2026-06-05.md) =====
# The ONLY genuinely-new deployable edge from a 2099-hypothesis latency-proof sweep (Chainlink-settled,
# hold-to-resolution, future-block held out, full-L2 cost-stressed, 4 adversarial skeptics).
# det_d12_wide = improved determinism: STRONGER distance gate (>=12bps vs running det_lwd's >=5) + WIDER
# window (last 180s vs 60s). The running det_lwd is ~break-even on fresh Chainlink (future -$0.06); this is
# future-POSITIVE +$1.04/tr [+0.45,+1.56] n=250, directionally balanced (UP +$2.06 / DOWN +$1.32), monotone
# in distance (real mechanism, not a fitted threshold), survives ALL-combined cost-stress +$1.12 [+0.69],
# and has the best capacity of any survivor (fills to $100/trade). Only 24% trade-overlap with running det.
# PAPER forward-test (1 week) before any live decision. Uses only existing DetParams gates (no engine change).
- id: det_d12_wide_v1
  name: "Determinism d12-wide (NEW sweep survivor: consistent, last 0-180s, dist>=12bps, ask 0.50-0.85, buy fav, hold; future +$1.04[0.45,1.56] CL, scales to $100)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "consistent", t_min_sec: 1, t_max_sec: 180, dist_min_bps: 12.0, min_ask: 0.50, max_ask: 0.85, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.85, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# ── TA-divergence APPROXIMATION twins (2026-06-16, PAPER) ──────────────────────────
# Live twins of research fam_ta_divergence (docs/research/TA_STRATEGIES_2026-06-16.md): the
# campaign's one new non-duplicate edge (CPCV 100%, latency-positive, dev +$7.8-9.5/tr, Jaccard
# <=0.21 vs deployed). Buy the 30s-spot-move side mid-window when |vel_bps(30)| >= ret_min and
# the 10s velocity agrees in sign (EMA-slope proxy), ask 0.30-0.55. APPROXIMATION (vel_bps vs
# strike, research ret is vs spot, <1% diff); parity-faithful mode="tadiv" is the queued follow-up
# (PART B of docs/superpowers/plans/2026-06-16-tadiv-engine-wiring.md). PAPER only (live:false).
# Forward gate before any live talk: >=7 clean days realized EV/fill CI-lower > 0. Two ret_min
# variants (3, 5) A/B the campaign's two best specs (ta_divergence_2585 / 2588).
- id: tadiv_approx_v1
  name: "TA-divergence approx ret5 (PAPER: buy 30s-move side, tl 60-300s, |move|>=5bps, 10s-vel agrees, ask 0.30-0.55; campaign spec ta_divergence_2588)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "tadiv_approx", tadiv_ret_min_bps: 5.0, t_min_sec: 60, t_max_sec: 300, dist_min_bps: 0.0, min_ask: 0.30, max_ask: 0.55, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.30, entry_price_max: 0.55, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
- id: tadiv_approx_ret3_v1
  name: "TA-divergence approx ret3 (PAPER: buy 30s-move side, tl 60-300s, |move|>=3bps, 10s-vel agrees, ask 0.30-0.55; campaign spec ta_divergence_2585)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "tadiv_approx", tadiv_ret_min_bps: 3.0, t_min_sec: 60, t_max_sec: 300, dist_min_bps: 0.0, min_ask: 0.30, max_ask: 0.55, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.30, entry_price_max: 0.55, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# LIVE twin of det_d12_wide_v1 (the paper entry above stays running for the OOS forward-test).
# Real-money executor bridge (live:true emits intents to data/live/intents.jsonl carrying
# bet_usd/bankroll_usd/max_daily_loss_usd). Runs ALONGSIDE det_lwd_live with its OWN isolated
# $100 bankroll + $25/day cap (the standalone executor keeps a separate book per strategy, so
# the two never interrupt each other). $5/trade. hard_worstcase = restart-safe daily cap.
- id: det_d12_wide_live
  name: "det_d12_wide LIVE (BACKUP 2026-06-09: superseded by det_d12_dual_live; kept as one-flag fallback — flip live:true to re-arm. consistent, last 0-180s, dist>=12bps, ask 0.50-0.85, buy fav, hold)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 100.0
  timeframe: "15m"
  determinism: {mode: "consistent", t_min_sec: 1, t_max_sec: 180, dist_min_bps: 12.0, min_ask: 0.50, max_ask: 0.85, max_daily_loss_usd: 25.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.85, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 5.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# ── det_d12 v2 (DUAL-ORACLE) — docs/research/DUAL_ORACLE_2026-06-09.md ──────────────────────
# Adds: oracle_gate="agree" (require Chainlink to AGREE with Coinbase at entry — the signal is
# Coinbase but Polymarket settles on Chainlink; cuts the near-strike FLIP losses that sank the
# 06-08→09 night), max_ask 0.85->0.78 (the 0.78-0.85 favourites pay little on win = low EV),
# adverse_vel_max_bps 2.0 (skip reverting spot). Chainlink-settled backtest 05-23..06-09:
# future +$1.97/tr[+1.31,+2.59] vs det_d12 baseline +$1.20 (+64%), WR 87%, CPCV 100%, DSR 0.996,
# survives 10s latency. cl_dist_bps comes live from ws_collector; on_missing="skip" = fail-closed.
- id: det_d12_dual_v1
  name: "Determinism d12 DUAL-ORACLE (paper A/B: AGREE gate + adaptive max_ask 0.78->0.85@|cl_dist|>=20 + adverse_vel2; CL future +$1.75, +17% vol vs flat0.78)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "consistent", t_min_sec: 1, t_max_sec: 180, dist_min_bps: 12.0, min_ask: 0.50, max_ask: 0.78, max_ask_hi: 0.85, cl_dist_hi_bps: 20.0, adverse_vel_max_bps: 2.0, oracle_gate: "agree", oracle_gate_on_missing: "skip", max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.78, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# LIVE primary (replaces det_d12_wide_live, which is kept above as a one-flag backup). Fresh $100
# isolated book, $5/trade, $25/UTC-day hard cap. The laddered fill (live_executor) chases only
# within [entry_ask, max_ask=0.78] — never overpays into the -EV band.
- id: det_d12_dual_live
  name: "det_d12 DUAL-ORACLE LIVE ($5/trade real money; AGREE gate + adaptive max_ask 0.78->0.85@|cl_dist|>=20 + adverse_vel2; $25/day hard cap, $100 bankroll)"
  enabled: false   # KILLED 2026-06-18 reckoning: -$103 (bankroll exhausted, self-halted 06-17); clean EV -$0.64/fill, even paper -$2.73. Fully off (overpay-above-cap defect).
  live: false
  type: determinism
  starting_capital_usd: 100.0
  timeframe: "15m"
  determinism: {mode: "consistent", t_min_sec: 1, t_max_sec: 180, dist_min_bps: 12.0, min_ask: 0.50, max_ask: 0.78, max_ask_hi: 0.85, cl_dist_hi_bps: 20.0, adverse_vel_max_bps: 2.0, oracle_gate: "agree", oracle_gate_on_missing: "skip", max_daily_loss_usd: 25.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.78, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 5.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# ===== NEW 2026-06-11 — settlement-print-model paper twins (mode="psettle") =====
# The frozen oracle-print logistic (data/research/oracle_print_model.json, fit on dev ticks
# only — ORACLE_PRINT_2026-06-10.md) is now evaluated LIVE in the engine
# (engine/print_model.py, parity-pinned to research p_settle by
# tests/research/test_print_model_parity.py). Both blocks are PAPER (live absent/false).
#
# psettle_ud_v1 = sweep spec psettle_2246 (SWEEP_LIVECOST_2026-06-10.md §5, artifact
# data/research/hypotheses/livecost_2026-06-10/specs.jsonl): healthy book, tl 60-360s,
# buy the NON-favourite ("cheap"/underdog side) at its YES-equivalent ask in [0.50, 0.78]
# when p_settle(that side) - ask >= 0.15 (d_thr; cl_floor 0 = no |cl_dist| gate), hold to
# resolution. Live-model future +$2.00/fill [+1.34,+2.64] n115 WR 75%, 5/5 days + 4/4
# coins positive, maxJaccard vs deployed <=0.13.
- id: psettle_ud_v1
  name: "psettle underdog-value paper (sweep psettle_2246: tl 60-360s, buy non-fav ask 0.50-0.78 when p_settle-ask>=0.15, hold; live-model future +$2.00/fill)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "psettle", psettle_side: "underdog", psettle_margin: 0.15, t_min_sec: 60, t_max_sec: 360, min_ask: 0.50, max_ask: 0.78, psettle_model_file: "data/research/oracle_print_model.json", psettle_on_missing: "skip", max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.50, entry_price_max: 0.78, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# oracle_fade_v1 = OP4 near-strike fade (ORACLE_PRINT_2026-06-10.md §OP4, G3 PASS):
# healthy book, tl 60-360s, book favourite's YES-equivalent ask >= 0.75 BUT
# p_settle(favourite) <= 0.60 -> buy the CHEAP side at its YES-equivalent ask, hold to
# resolution. OP4 registered cheap ask <= 0.35 (executor ceiling 0.40); PAPER twin entry
# band [0.05, 0.35] per the deployment spec. Live-model future +$5.87/fill [+4.50,+7.30]
# n271 (seeds0-4 $5.92+-0.56), 9/9 future days positive, 4/4 coins, Jaccard vs
# fav_disagree 0.101. Known optimism: random zero-fill hazard vs adversely-selected live
# misses — this paper run prices that haircut.
- id: oracle_fade_v1
  name: "Oracle-print near-strike fade paper (OP4: tl 60-360s, fav>=0.75 but p_settle(fav)<=0.60, buy cheap side ask 0.05-0.35, hold; live-model future +$5.87/fill)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "psettle", psettle_side: "fade", psettle_fav_ask_min: 0.75, psettle_p_fav_max: 0.60, t_min_sec: 60, t_max_sec: 360, min_ask: 0.05, max_ask: 0.35, psettle_model_file: "data/research/oracle_print_model.json", psettle_on_missing: "skip", max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.05, entry_price_max: 0.35, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
# ===== NEW 2026-06-12 — 5m<->15m cross-book CAUSAL twin (mode="xb") =====
# MEASURING an unproven causal variant — NOT confirming a known number. The original XI4
# headline (+$1.36 lg / +$1.84 v2 future) was RETRACTED: the research frame back-filled the
# 5m strike (k5, discovery start_price, captured median ~24s / p90 ~55s AFTER the 5m open)
# to ALL ticks, so 74% of its decision ticks fired BEFORE the strike they condition on
# existed (see docs/research/test_ledger.md § "XI4 AMENDMENT (2026-06-12)"). This twin is
# the re-registered CAUSAL variant: same rule, entries only at/after the k5 capture — the
# engine's NATURAL behavior (ws_collector emits xb5_k5 = NaN until discovery captures the
# co-terminal 5m strike; every NaN fails closed, so the engine physically cannot fire
# acausally). PAPER-FORWARD is the only valid evidence — the 18-day history is acausal for
# this rule, so no backtest reveal can bless it; this block's job is to MEASURE.
# Priors (honest): UNPROVEN, ~+$0.95/fill v2 in the s5>=15 sensitivity slice, CI
# [-0.10,+1.99] SPANS ZERO. Causal-restricted history rate ~19 sig/day (336 slugs /
# 05-23..06-09; slug-Jaccard 0.870 vs the retracted frame — most violation windows keep
# firing after capture, so the slug set survives even though 74% of decision TICKS were
# acausal). RULE (research/analysis/external_inputs.py XI4, chosen m=0.03/g=2bps): in the
# last 300s of a 15m window the co-terminal 5m market (same close, strike K5 = spot at
# 15m_start+600s) bounds the 15m price by strike monotonicity; when the bound is violated
# at EXECUTABLE quotes — 15m side's YES-equiv ask + 0.03 <= the 5m implied side's bid,
# |K5 vs K15| >= 2bps, >= $1 notional behind the referenced 5m quote — buy that 15m side
# at ask <= 0.90 (no floor: the rule buys CHEAP sides, median ask ~0.44) and hold to
# resolution. Parity pins: tests/research/test_xb_parity.py (decision Jaccard vs the
# causal frame, co5 feature parity through the real _emit_for_second).
# GATE FOR ANY LIVE DECISION: >= 7 forward paper days AND realized EV/fill with bootstrap
# CI-lower > 0. Until then this stays live:false regardless of how good the tape looks.
- id: xb_5m15m_causal_v1
  name: "5m<->15m cross-book CAUSAL paper (XI4 m0.03/g2bps, entries only post-k5-capture by construction; UNPROVEN ~+$0.95/fill v2-sens CI spans 0 — measuring, paper-forward only)"
  enabled: true
  live: false
  type: determinism
  starting_capital_usd: 1000.0
  timeframe: "15m"
  determinism: {mode: "xb", xb_premium: 0.03, xb_gap_min_bps: 2.0, xb_min_ref_usd: 1.0, xb_on_missing: "skip", t_min_sec: 5, t_max_sec: 300, min_ask: 0.0, max_ask: 0.90, max_daily_loss_usd: 50.0, daily_loss_mode: "hard_worstcase"}
  sim_config:
    entry: {side: "BOTH", entry_price_min: 0.0, entry_price_max: 0.90, drop_magnitude_pct: 0.0, drop_window_sec: 30, min_time_left_sec: 0, proximity_max_pct: 100.0, min_seconds_into_window: 0}
    exit:  {profit_target_pct: 0.0, stop_loss_pct: null, max_hold_sec: 900, trailing_stop_pct: null}
    filter: {min_book_depth_usd: 0.0, max_spread: 1.0, book_imbalance_min: null, vol_regime: "ALL", time_of_day: "ALL", multi_tier_entry: 1, correlated_signal_filter: false}
    human: {reaction_delay_min_sec: 0.0, reaction_delay_max_sec: 0.0, signal_skip_prob: 0.0, daily_trade_cap: null, post_loss_cooldown_sec: 0, concurrent_position_cap: 50, fixed_bet_usd: 10.0}
    fill:  {fee_rate: 0.07, reject_prob: 0.0, use_next_tick_for_fill: true, realistic_fill_model: true}
